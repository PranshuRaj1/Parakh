---
priority: high
scope: fixtures/**
---

# Fixture conventions

- Every fixture file must end its filename with a `_fixture` suffix before the extension
- Never import production modules from fixture files; keep them self-contained
